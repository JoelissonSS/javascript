// Crie 4 li's na página
// Utilizando o for...of
// adicione uma classe a cada li
// const btns = document.querySelectorAll('li')

// for(const btn of btns){
//   btn.classList.add('buttonss')
// }
// Utilize o for...in para listar
// todos as propriedades e valores
// do objeto window
const w = window
console.log(w)
for (const i in w){
  console.log(i, w[i])
}